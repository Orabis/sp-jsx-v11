export interface inputInterface {
  labelName: string;
  placeholder: string;
  isReadOnly: boolean;
}
export interface buttonInterface {
  buttonType: 'submit' | 'reset' | 'button';
  buttonText: string;
}